const prompt = require("prompt-sync")();
let numero = prompt("Digite um número:");
let raiz = Math.sqrt(numero); //é a abreviação de "square root" (raiz quadrada em inglês)

console.log("A raiz quadrada é:", raiz);